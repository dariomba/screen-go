package app

type Container struct {
	// Add shared dependencies here, e.g.:
	// Params
	// Services
}

func NewContainer() *Container {
	return &Container{
		// Initialize shared dependencies here
	}
}
